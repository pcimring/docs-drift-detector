[project]
name = "docs-drift-detector"
version = "0.1.0"
dependencies = [
    "psycopg[binary]>=3.1,<4",
    "markdown-it-py>=3.0,<4",
    "pyyaml>=6.0,<7",
    "python-dotenv>=1.0,<2",
    "requests>=2.31,<3",
    "anthropic>=0.40,<1",
]

[tool.vercel]
entrypoint = "api.check:handler"
